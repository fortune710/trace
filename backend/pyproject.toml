[project]
name = "trace-backend"
version = "0.1.0"
description = "Backend APIs for Trace API endpoints"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.53.0",
]
